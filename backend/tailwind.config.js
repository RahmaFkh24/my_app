/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    theme: {
      extend: {
        colors: {
          primary: 'hsl(var(--primary))',
          // Define other colors with explicit values
        }
      }
    }
  },
  plugins: [],
}

